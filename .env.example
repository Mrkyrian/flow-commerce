# Supabase project URL — must be a full http(s) URL, not an API key.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anon / publishable API key (Project Settings → API).
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
